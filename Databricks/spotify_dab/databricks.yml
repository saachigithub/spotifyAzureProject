# This is a Declarative Automation Bundle definition for spotify_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dab
  uuid: ad8dd967-8bda-4895-8400-9eb22ed3306e




targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405616144891782.2.azuredatabricks.net
    presets:
      source_linked_deployment: false

  prod:
    mode: production
    workspace:
      host: https://adb-7405616144891782.2.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/great786parth@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/PROD/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: great786parth@gmail.com
        level: CAN_MANAGE
